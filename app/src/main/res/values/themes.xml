<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/colorPrimary</item>
    </style>

    <!-- Custom theme to remove the status bar -->
    <style name="NoStatusBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Custom BottomSheetDialog style -->
    <style name="TransparentBottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/TransparentBottomSheet</item>
    </style>

    <style name="TransparentBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Theme.APISETUP.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.APISETUP.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.APISETUP.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="EditTextNoUnderline">
        <item name="android:background">@null</item>
    </style>

    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_theme</item>
        <!--        <item name="android:windowBackground">@drawable/splash_theme</item>-->
        <item name="colorPrimary">@color/splash_screen_gradint_2</item>
        <item name="android:statusBarColor">@color/splash_screen_gradint_1</item>
    </style>

    <style name="TabtextAppearence" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="textAllCaps">false</item>
        <!--        <item name="android:textStyle">bold</item>-->
    </style>

    <style name="TabtextAppearence2" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">12sp</item>
        <item name="textAllCaps">false</item>
        <!--        <item name="android:textStyle">bold</item>-->
    </style>


    <style name="SwitchTheme" parent="Theme.AppCompat.Light">
        <!--        <item name="android:colorControlActivated">@color/switch_theme_color</item>-->
        <item name="colorControlActivated">@color/brand_color</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#f1f1f1
        </item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#42221f1f</item>
    </style>


    <style name="progressBarBlue" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/main_adapter_background_color_comp1</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="progressBarBlue2" parent="@style/Theme.AppCompat">
        <item name="colorAccent">@color/white</item>
    </style>

</resources>